{
  "name": "frontfacer-tunnel-backend",
  "private": true,
  "type": "module",
  "description": "Netlify Functions backend for FRONTFACER Tunnel (replaces Firebase Cloud Functions + Firestore).",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@netlify/blobs": "^8.3.0"
  }
}
